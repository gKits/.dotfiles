# Leader
set $mod Mod4

# Direction Keys
set $left h
set $down j
set $up k
set $right l

# Terminal emulator
set $term alacritty

# Locker
set $lock ~/.local/bin/blurlock

# Application launcher
set $menu wofi --show drun

include ./config.d/*
